import { MessageToInputPipe } from './message-to-input.pipe';

describe('MessageToInputPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageToInputPipe();
    expect(pipe).toBeTruthy();
  });
});
